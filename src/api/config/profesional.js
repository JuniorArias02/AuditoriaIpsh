import { API_BASE } from "./apiBase";

export const PROFESIONAL = {
	LISTAR: `${API_BASE}/profesional`,
	OBTENER: `${API_BASE}/profesional/`,
	OBTENER_POR_NOMBRE_CEDULA: `${API_BASE}/profesional/filtro`,
	CREAR: `${API_BASE}/profesional`,
	ACTUALIZAR: `${API_BASE}/profesional`,

};
